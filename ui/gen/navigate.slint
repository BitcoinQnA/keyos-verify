// Generated by `slint-keyos-platform` DO NOT EDIT!

export enum Animate {
    Forward,
    Backward,
    None,
}

struct NavigateOptions {
    // Replace the current route in the history stack
    replace: bool,
    
    // Animation to use when navigating
    animate: Animate,
}

export struct PublisherPageParams {
    details-available: bool, 
    email: string, 
    fingerprint: string, 
    key-available: bool, 
    name: string, 
}

export global Navigate {
    // Navigation state
    in property <bool> has-backward;
    in property <bool> has-forward;
    
    // Generic navigation callbacks
    callback backward();
    callback forward();
    
    callback backward-animate(Animate);
    callback forward-animate(Animate);
    
    callback return-home();
    callback return-home-animate(Animate);
    
    // Log the current navigation state
    callback debug();
    
    // Raw string navigation
    callback navigate(string, NavigateOptions);
    
    //
    // Type safe navigation callbacks
    //
    
    // 
    callback main-page(NavigateOptions);
    
    // /about
    callback about-page(NavigateOptions);
    
    // /compare
    callback compare-page(NavigateOptions);
    
    // /expected
    callback expected-page(NavigateOptions);
    
    // /hash
    callback hash-page(NavigateOptions);
    
    // /key-picker
    callback key-picker-page(NavigateOptions);
    
    // /keys
    callback keys-page(NavigateOptions);
    
    // /publisher?{fingerprint}&{name}&{email}&{details_available}&{key_available}
    callback publisher-page(PublisherPageParams, NavigateOptions);
    
    // /release
    callback release-page(NavigateOptions);
    
    // /result
    callback result-page(NavigateOptions);
    
    // /trust
    callback trust-page(NavigateOptions);
}

export global RoutePath {
    out property <string> main-page : "";
    out property <string> about-page : "/about";
    out property <string> compare-page : "/compare";
    out property <string> expected-page : "/expected";
    out property <string> hash-page : "/hash";
    out property <string> key-picker-page : "/key-picker";
    out property <string> keys-page : "/keys";
    pure callback publisher-page(PublisherPageParams) -> string;
    out property <string> release-page : "/release";
    out property <string> result-page : "/result";
    out property <string> trust-page : "/trust";
}

